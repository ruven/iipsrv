These batch files are called after the installation (immediately after files were copied),
to configure everything (Install IIS, configure virtual directory, create url shorcuts etc.).

For closer information read documentation and comments in batch files and also in scripts.